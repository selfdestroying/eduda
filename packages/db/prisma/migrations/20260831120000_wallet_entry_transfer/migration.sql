-- Перенос пакета между кошельками: пара строк журнала, минус на источнике и плюс
-- на получателе.
--
-- Отдельной миграцией и без других операторов: Postgres не разрешает использовать
-- значение enum в той же транзакции, где оно добавлено, а Prisma заворачивает
-- каждую миграцию в транзакцию.
ALTER TYPE "WalletEntryKind" ADD VALUE 'TRANSFER';
