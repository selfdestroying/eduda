# Prettier ищет ignore-файл относительно cwd, а корневой .prettierignore
# при запуске `pnpm --filter platform ...` не подхватывается — дублируем нужное.
.next
out
public/uploads

# Генерируется `next typegen` неотформатированным — на гонке с ним
# `format:check` падает, и `pnpm check` отдаёт ненулевой код.
*.d.ts
next-env.d.ts
