/**
 * Реализация переехала в `@repo/core/features`: реестр читает и `apps/bots` —
 * планировщик пропускает школы с выключенной фичей `notifications`.
 *
 * Путь оставлен реэкспортом ради восьми импортов в платформе. `effective.ts`
 * рядом остаётся здесь: он ходит в базу и платформе принадлежит целиком.
 */
export * from '@repo/core/features'
