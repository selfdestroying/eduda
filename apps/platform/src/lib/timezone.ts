/**
 * Реализация переехала в `@repo/core/timezone`: время напоминания родителям
 * считает `apps/bots`, а пояс — свойство школы, и вторая копия этих функций
 * разъехалась бы с первой молча.
 *
 * Путь оставлен реэкспортом ради 82 импортов в платформе — тем же приёмом
 * здесь же реэкспортируется `cn` из `@repo/ui`.
 */
export * from '@repo/core/timezone'
