/**
 * Ответ роута. Тело всегда текст: VK ждёт `ok` или строку подтверждения, MAX
 * не смотрит в тело вовсе, а `/dispatch` читают глазами в терминале — JSON тут
 * не нужен никому.
 */
export type Reply = { status?: number; text: string }
